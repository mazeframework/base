# base
base modular application recipe

Used as the default application recipe when no recipe argument is provided.  It can also be explicitely used with the command
```
maze new myapp -r mazeframework/base
```

